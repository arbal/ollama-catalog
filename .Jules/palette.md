## 2024-05-24 - CLI Empty States with Rich
**Learning:** Users can find empty CLI tables confusing and unhelpful when running complex filter commands.
**Action:** When a search or filter yields no results in a CLI app using the 'rich' library, display a helpful Panel containing the active filters rather than an empty table.
