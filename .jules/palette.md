## 2025-02-23 - Better Empty States in CLI
**Learning:** Empty tables in CLI apps without rows can feel broken or confusing to users.
**Action:** Always render an explicit empty state Panel in `rich` when a collection is empty, showing active filters so users know why they got no results.
